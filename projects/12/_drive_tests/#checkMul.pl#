#!/usr/bin/perl -w
use strict;
my $fileName = shift;

open(my $IN, "<", $fileName) or die "Can not open file $fileName\n";

my $inDarwPixel = 0;
while (my $line = <$IN>){
#  print $line;
  chomp($line);

  
  
}
